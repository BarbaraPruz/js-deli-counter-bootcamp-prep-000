var katzDeli = [];

function takeANumber ( arr, name) {
  arr.push(name);
  return arr.length;
}

function nowServing ( arr ) {
  if (arr.length === 0) {
    return 'There is nobody waiting to be served!';
  }
  
}